version: "3.8"

services:
  webserver:
    depends_on:
      - certbot
      - next

  next:
    depends_on:
      - django
      - celery
      - celery_beat

  cypress:
    environment:
      - CYPRESS_baseUrl=https://www.modularhistory.com/
