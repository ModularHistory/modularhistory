name: "Test"
description: "Run ModularHistory's test suite."
runs:
  using: "composite"
  steps:
    # Run tests
    - name: Run tests
      shell: bash
      run: |
        python manage.py migrate
        python manage.py test
